package enums;

public enum PickupType {
    DAMAGE_BUFF,
    MOVEMENT_BUFF,
    ROF_BUFF,
    HEALTHPACK,

    //can have one for each weapon pickup
    BULLET_PROJECTILE,
    BULLET_HITSCAN,
    BULLET_LASER,

    //ammo if required
    AMMO
}
